blit camera revamp;
    currently;
        each blit happens to draw whether or not the entity or ui component is within the camera's view space
        strengths;
            it's fast
            easy to implement
            efficient
        weaknesses;
            hard to manage visibility
            has no logical way to determine if the object is fully contained or not due to the blits hierarchy
            cannot determine the exact space of objects to draw

    revamp;
        the camera revamp will guarantee the exact pixel space of the images sent to the blits callbacks
        by using the camera's position and size to determine the exact pixel bounds of the pyoneer objects
        and then only drawing the object's exact pixel bounds that are within the camera's view space
        strengths;
            easy to manage visibility
            can determine if the object is fully contained or not
            can determine the exact space of objects to draw

    currently the hierarchy is too complicated to simply add a camera to the blits;
        revamps necessary;
            draw component;
                draw component is the base class for all drawable objects
                    currently it does not perform it's task correctly
                    it should be the only class that can draw to the screen and should have managed draw calls only
                    currently the concept is used here;
                        entity
                    it must be implemented in;
                        shape
                        text
                        image


revamps;
    static library concept;
        currently the library is semi-dynamic and must be instantiated at runtime
        future refactor for currently dynamic library is to have a single object that is the entire encompassing global library
        each library will access this module for all necessary code and importations

widgets;
    X keyboard;
        needs rewrite; currently only keydown/keyup/repeat events
    X mouse;
        # rewritten; supports many more events and directly attaches to components
    X async expansion;
        needs rewrite
    X label;
    X textbox;
    X button;
    # icon;
        # drawing a simple icon
    popup menu;
        mouse;
            right click opens attached menu

    window; blocked until panel is complete
        needs resize and move
    panel;
        grid; in progress
            a functional grid lineup component for organization
        scroll; in progress
            X active space;
                # contains an active space primarily for scrolling, visual, and interactive purposes
            keyboard control;
                blocked; active selection needs implementing
                    directions; up, down, left, right
            X mouse control; in progress
                X clicks; left, right, middle
                drag direction;
                    drag from point A to B to scroll
            X scroll bar control
                X click down on the thumb;
                    X drag up or down to scroll
                X click empty space in the bar;
                    X scroll up or down one page
                X click up or down arrow;
                    X scroll up or down one line
        blocking;
            listbox



    listbox:
        a simple list or grid containing various simple components with text
