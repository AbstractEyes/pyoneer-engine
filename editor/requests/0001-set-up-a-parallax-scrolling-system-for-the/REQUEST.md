# Set up a parallax scrolling system for the

6 notes across 1 scope.

## `project`

*Likely files:* `main.py`, `editor/core/project.py`, `docs/PLAN_EDITOR.md`

- Set up a parallax scrolling system for the background with a speed, make it so it only scrolls a little and then returns.
- Add a button to unstage requests.
- Parallax x is different but does not change the parallax background, there is also no parallax background pathway or systems installed for pathway altering.
- Still cannot add or remove tilesets.
- Terrain tilesets not selectable.
- Tileset size and constraints still not set.
