<!-- pyoneer-doc: L1 -->
<!-- GENERATED by tools/gen_map.py. Do not edit. Regenerate: .venv/Scripts/python.exe tools/gen_map.py --write -->

# `scripts/core/audio.py` — tier 2 #TAG:scripts/core/audio.py

> Where the engine looks for a sound, and what it does when there is no card.

`scripts.core.audio` · 645 lines · tier 1: [`../MAP.md`](../MAP.md)

**First-party imports.** `scripts/` may never import `editor/` — this line is where that is auditable.

    config.managers.core_asset_manager scripts.core.errors

## Module constants

- `scripts/core/audio.py:105` `REPO_ROOT` #TAG:audio.REPO_ROOT
- `scripts/core/audio.py:108` `SOUND_ROOT` #TAG:SOUND_ROOT
- `scripts/core/audio.py:111` `AUDIO_ROOT` #TAG:AUDIO_ROOT
- `scripts/core/audio.py:124` `AUDIO_SUFFIXES` #TAG:AUDIO_SUFFIXES
- `scripts/core/audio.py:253` `MIXER_SIZES` #TAG:MIXER_SIZES
- `scripts/core/audio.py:256` `MIXER_CHANNELS` #TAG:MIXER_CHANNELS
- `scripts/core/audio.py:259` `CONFIG_PATH` #TAG:CONFIG_PATH
- `scripts/core/audio.py:262` `SETTING_KEYS` #TAG:SETTING_KEYS

## Functions

- `scripts/core/audio.py:132` `_parts(path: str) -> Tuple[str, ...]` #TAG:audio._parts
  - Split a path on either separator, dropping empties.
- `scripts/core/audio.py:142` `shipped_audio(relative: str) -> str` #TAG:shipped_audio
  - `data/sound/X` -> `data/audio/X`, still repo-relative.
- `scripts/core/audio.py:158` `shipped_audio_path(relative: str) -> str` #TAG:shipped_audio_path
  - Where `shipped_audio(relative)` lands in THIS checkout, absolutely.
- `scripts/core/audio.py:163` `_under_repo(path: str) -> str` #TAG:audio._under_repo
  - `path` absolute, resolving a relative one against the REPO, not the cwd.
- `scripts/core/audio.py:175` `declared_audio(name: str, where: str='') -> str` #TAG:declared_audio
  - A root-free authored name -> the path the OVERRIDE root would hold.
- `scripts/core/audio.py:205` `resolve_audio(path: str) -> str` #TAG:resolve_audio
  - The declared path if anything is there, else its shipped twin.
- `scripts/core/audio.py:230` `available_names() -> Tuple[str, ...]` #TAG:available_names
  - Every root-free name either root actually holds, sorted and unique.
- `scripts/core/audio.py:283` `_whole(config: Mapping[str, Any], key: str, where: str) -> int` #TAG:_whole
  - One integer setting, refusing a bool and refusing a float.
- `scripts/core/audio.py:299` `read_settings(config: Any, where: str=CONFIG_PATH) -> MixerSettings` #TAG:read_settings
  - Judge an authored audio config, or raise naming the key and the file.
- `scripts/core/audio.py:364` `_judged_volume(volume: Any, where: str, op: str) -> float` #TAG:_judged_volume
  - A per-call volume in 0.0..1.0, or raise. Clamping would hide a typo.

## Classes

### `@dataclass(frozen=True) class MixerSettings` #TAG:MixerSettings

`scripts/core/audio.py:268`–`280`

> The five numbers `pygame.mixer.init` is opened with, already judged.

- (no methods)

### `class AudioManager(Singleton)` #TAG:AudioManager

`scripts/core/audio.py:385`–`636`

> One mixer, one cache of loaded Sounds, one answer about the device.

- `scripts/core/audio.py:405` `__init__(self) -> None` #TAG:AudioManager.__init__
- `scripts/core/audio.py:421` `prepare(self, config: Any) -> 'AudioManager'` #TAG:AudioManager.prepare
  - Judge the config, open the mixer on it, and say what happened.
- `scripts/core/audio.py:470` `shutdown(self) -> None` #TAG:AudioManager.shutdown
  - Close the mixer and forget every loaded Sound.
- `scripts/core/audio.py:487` `@property available(self) -> bool` #TAG:AudioManager.available
  - True when the mixer is open. False is a silent machine, not a bug.
- `scripts/core/audio.py:492` `@property unavailable_reason(self) -> str` #TAG:AudioManager.unavailable_reason
  - Why `available` is False, in words. Empty while it is True.
- `scripts/core/audio.py:497` `@property prepared(self) -> bool` #TAG:AudioManager.prepared
  - True once `prepare` has judged a config, whatever the device said.
- `scripts/core/audio.py:502` `@property master_volume(self) -> float` #TAG:AudioManager.master_volume
  - The authored master scale, or 1.0 while nothing is prepared.
- `scripts/core/audio.py:507` `@property loaded(self) -> Tuple[str, ...]` #TAG:AudioManager.loaded
  - Every resolved path in the Sound cache, sorted. For a check.
- `scripts/core/audio.py:513` `locate(self, name: str, where: str='') -> str` #TAG:AudioManager.locate
  - The path a root-free name resolves to, or RAISE naming the name.
- `scripts/core/audio.py:530` `sound(self, name: str, where: str='') -> Optional[pygame.mixer.Sound]` #TAG:AudioManager.sound
  - The cached `Sound` for a name, or None when there is no device.
- `scripts/core/audio.py:547` `play_sound(self, name: str, volume: float=1.0, where: str='') -> bool` #TAG:AudioManager.play_sound
  - Start one effect. True when a channel took it.
- `scripts/core/audio.py:562` `play_music(self, name: str, loops: int=0, volume: float=1.0, where: str='') -> bool` #TAG:AudioManager.play_music
  - Start the streamed track. True when the stream started.
- `scripts/core/audio.py:586` `stop_music(self) -> None` #TAG:AudioManager.stop_music
  - Stop the stream. Safe with no device and with nothing playing.
- `scripts/core/audio.py:594` `@property music_name(self) -> str` #TAG:AudioManager.music_name
  - The last track `play_music` started, or "" if there was none.
- `scripts/core/audio.py:599` `@property music_playing(self) -> bool` #TAG:AudioManager.music_playing
  - True while a track this manager started is still streaming.
- `scripts/core/audio.py:605` `@property music_finished(self) -> bool` #TAG:AudioManager.music_finished
  - True once a track this manager started has stopped.
- `scripts/core/audio.py:621` `_require_prepared(self, action: str) -> None` #TAG:AudioManager._require_prepared
  - Refuse to pretend when nobody wired this up.
